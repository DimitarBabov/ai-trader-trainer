# config.py
# Global configuration for ticker and timeframe

TICKER = "SLV"          # Define the ticker symbol, e.g., "SLV" or "MSFT"
TIMEFRAME = "1d"        # Define the timeframe, e.g., "1d", "1wk", "1mo"
BATCH_SIZE = 32         # Batch size for data loading and training
LEARNING_RATE = 0.001   # Learning rate for the optimizer
NUM_EPOCHS = 10         # Number of epochs for training
